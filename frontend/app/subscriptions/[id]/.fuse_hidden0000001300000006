import { redirect } from "next/navigation";

/** See app/subscriptions/page.tsx — superseded by the Plans catalog, which has
 *  no per-customer-subscription drill-down (see frontend README known gaps). */
export default function SubscriptionDetailRedirect() {
  redirect("/plans");
}
