/**
 * Deprecated — superseded by components/plans/plan-form.tsx and
 * app/plans/page.tsx (the "Subscriptions" tab was renamed to "Plans").
 * Kept as an empty module rather than deleted because this outputs
 * folder won't allow deleting files once written; nothing imports this.
 */
export {};
